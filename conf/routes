GET /index controllers.Wallet.index
GET / controllers.Wallet.index

GET /accounts/$account<\w{1,20}> controllers.Wallet.amount(account : String)

POST /add controllers.Wallet.add(account : String, summ : Int)

POST /move controllers.Wallet.move(from : String, to : String, summ : Int)