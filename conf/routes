GET /index controllers.Wallet.index
GET / controllers.Wallet.index

GET /amount controllers.Wallet.amount(account : String)

GET /add controllers.Wallet.add(account : String, summ : Int)

GET /move controllers.Wallet.move(from : String, to : String, summ : Int)